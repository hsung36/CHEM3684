name: docs_montecarlo
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python=3.13.1
  - pip
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton




    # Pip-only installs
  - pip:
    - -e ../

