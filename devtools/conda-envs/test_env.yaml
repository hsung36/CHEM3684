name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.13.1
  - pip

    # Testing
  - pytest
  - pytest-cov
  - numpy
  - networkx
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

